name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "0 2 * * *"
jobs:
  update-readme:
    name: Update Readme with Credly badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: mSaurabh/mSaurabh@main
        with:       
          CREDLY_USER: saurabh-mankar.1bfc8e95 # optional, but default will use the same from github
